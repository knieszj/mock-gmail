let SearchEmails = (props) => {
    return (
        <>
            {/*    In order to Link to an email that I search I need to "useRouteMatch" but that requires React Hooks which is only used in a functional component.  The search capability requires State which is a Class component ability.*/}
        </>
    )
}
export default SearchEmails